package mod.industrialProcessing.power.utils;

import net.minecraftforge.common.util.ForgeDirection;

public interface IMechanicalMotionReceiver {
	/***
	 * Adds torque to the system, get torque in return
	 * @param side
	 * @param torque
	 * @return
	 */
	//float addTorque(ForgeDirection side, float torque); 
}
